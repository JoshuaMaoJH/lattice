name: lattice_runtime
description: The thin reactive runtime that Lattice-generated Flutter projects depend on. Zero non-Flutter dependencies.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.6.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
