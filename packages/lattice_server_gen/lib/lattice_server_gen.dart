/// Emits the server side of a Lattice project's `@server` subgraphs, and the
/// typed client stubs that call it (§6, §7.7).
library;

export 'src/server_emitter.dart';
export 'src/server_generator.dart';
