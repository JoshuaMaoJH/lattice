name: lattice_server_gen
description: Emits a runnable Dart server from a project's server functions, plus the typed client stubs that call it.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  code_builder: ^4.10.0
  dart_style: ^3.0.0
  lattice_codegen: ^0.1.0
  lattice_core: ^0.1.0
  path: ^1.9.0

dev_dependencies:
  lints: ^5.1.0
  test: ^1.25.0
