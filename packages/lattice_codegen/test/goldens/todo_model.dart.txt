// Generated by Lattice from models/.
//
// Plain immutable data classes — no annotations, no build_runner, nothing to
// re-run after ejecting.

final class Todo {
  const Todo({
    required this.id,
    required this.title,
    this.done = false,
    required this.tags,
    this.dueAt,
  });

  factory Todo.fromJson(Map<String, Object?> json) => Todo(
    id: json['id'] as String,
    title: json['title'] as String,
    done: json['done'] == null ? false : json['done'] as bool,
    tags: [for (final e in json['tags'] as List<Object?>) e as String],
    dueAt: json['dueAt'] == null ? null : json['dueAt'] as String,
  );

  final String id;

  final String title;

  final bool done;

  final List<String> tags;

  final String? dueAt;

  Map<String, Object?> toJson() => {
    'id': id,
    'title': title,
    'done': done,
    'tags': tags,
    'dueAt': dueAt,
  };

  Todo copyWith({
    String? id,
    String? title,
    bool? done,
    List<String>? tags,
    String? dueAt,
  }) => Todo(
    id: id ?? this.id,
    title: title ?? this.title,
    done: done ?? this.done,
    tags: tags ?? this.tags,
    dueAt: dueAt ?? this.dueAt,
  );

  @override
  bool operator ==(Object other) =>
      other is Todo &&
      other.id == id &&
      other.title == title &&
      other.done == done &&
      _listEquals(other.tags, tags) &&
      other.dueAt == dueAt;

  @override
  int get hashCode => Object.hash(id, title, done, Object.hashAll(tags), dueAt);

  @override
  String toString() =>
      'Todo(id: $id, title: $title, done: $done, tags: $tags, dueAt: $dueAt)';
}

bool _listEquals<T>(List<T>? a, List<T>? b) {
  if (identical(a, b)) return true;
  if (a == null || b == null || a.length != b.length) return false;
  for (var i = 0; i < a.length; i++) {
    if (a[i] != b[i]) return false;
  }
  return true;
}
