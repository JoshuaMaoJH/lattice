// Generated by Lattice from models/.
//
// Plain immutable data classes — no annotations, no build_runner, nothing to
// re-run after ejecting.

final class CurrentWeather {
  const CurrentWeather({
    required this.time,
    required this.temperature,
    required this.windspeed,
    required this.weathercode,
  });

  factory CurrentWeather.fromJson(Map<String, Object?> json) => CurrentWeather(
    time: json['time'] as String,
    temperature: (json['temperature'] as num).toDouble(),
    windspeed: (json['windspeed'] as num).toDouble(),
    weathercode: json['weathercode'] as int,
  );

  final String time;

  final double temperature;

  final double windspeed;

  final int weathercode;

  Map<String, Object?> toJson() => {
    'time': time,
    'temperature': temperature,
    'windspeed': windspeed,
    'weathercode': weathercode,
  };

  CurrentWeather copyWith({
    String? time,
    double? temperature,
    double? windspeed,
    int? weathercode,
  }) => CurrentWeather(
    time: time ?? this.time,
    temperature: temperature ?? this.temperature,
    windspeed: windspeed ?? this.windspeed,
    weathercode: weathercode ?? this.weathercode,
  );

  @override
  bool operator ==(Object other) =>
      other is CurrentWeather &&
      other.time == time &&
      other.temperature == temperature &&
      other.windspeed == windspeed &&
      other.weathercode == weathercode;

  @override
  int get hashCode => Object.hash(time, temperature, windspeed, weathercode);

  @override
  String toString() =>
      'CurrentWeather(time: $time, temperature: $temperature, windspeed: $windspeed, weathercode: $weathercode)';
}

final class Forecast {
  const Forecast({
    required this.latitude,
    required this.longitude,
    required this.currentWeather,
  });

  factory Forecast.fromJson(Map<String, Object?> json) => Forecast(
    latitude: (json['latitude'] as num).toDouble(),
    longitude: (json['longitude'] as num).toDouble(),
    currentWeather: CurrentWeather.fromJson(
      json['current_weather'] as Map<String, Object?>,
    ),
  );

  final double latitude;

  final double longitude;

  final CurrentWeather currentWeather;

  Map<String, Object?> toJson() => {
    'latitude': latitude,
    'longitude': longitude,
    'current_weather': currentWeather.toJson(),
  };

  Forecast copyWith({
    double? latitude,
    double? longitude,
    CurrentWeather? currentWeather,
  }) => Forecast(
    latitude: latitude ?? this.latitude,
    longitude: longitude ?? this.longitude,
    currentWeather: currentWeather ?? this.currentWeather,
  );

  @override
  bool operator ==(Object other) =>
      other is Forecast &&
      other.latitude == latitude &&
      other.longitude == longitude &&
      other.currentWeather == currentWeather;

  @override
  int get hashCode => Object.hash(latitude, longitude, currentWeather);

  @override
  String toString() =>
      'Forecast(latitude: $latitude, longitude: $longitude, currentWeather: $currentWeather)';
}
