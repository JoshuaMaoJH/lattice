// Generated by Lattice from page_home.
//
// Safe to edit once you have ejected: this is ordinary Flutter code with no
// runtime dependency on the editor. Node ids appear as comments so a change
// here can still be traced back to the graph.

import 'dart:convert';

import 'package:flutter/material.dart';
import 'package:http/http.dart' as http;
import 'package:signals_flutter/signals_flutter.dart';

import '../models.dart';
import '../custom/cities.dart';

class HomePage extends StatefulWidget {
  const HomePage({super.key});

  @override
  State<HomePage> createState() => _HomePageState();
}

class _HomePageState extends State<HomePage> {
  // n_city
  final city = signal<String>('Berlin');

  // n_forecast
  final forecast = signal<Forecast?>(null);

  // n_loading
  final loading = signal<bool>(false);

  // n_error
  final error = signal<String?>(null);

  // w_city
  late final TextEditingController _cityController;

  late final void Function() _cityControllerSync;

  @override
  void initState() {
    super.initState();
    _cityController = TextEditingController(text: city.value);
    _cityControllerSync = effect(() {
      final next = city.value;
      if (_cityController.text != next) {
        _cityController.text = next;
      }
    });
  }

  @override
  void dispose() {
    _cityControllerSync();
    _cityController.dispose();
    super.dispose();
  }

  // ev_city -> a_city
  void _onCityChanged(String value) {
    city.value = value;
  }

  // ev_fetch -> a_fetch
  Future<void> _onFetchPressed() async {
    loading.value = true;
    error.value = null;
    try {
      final response = await http.get(Uri.parse(_urlFor(city.value)));
      if (response.statusCode >= 400) {
        throw Exception('Request failed: ${response.statusCode}');
      }
      forecast.value = Forecast.fromJson(
        jsonDecode(response.body) as Map<String, Object?>,
      );
    } catch (failure) {
      error.value = failure.toString();
    } finally {
      loading.value = false;
    }
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Weather')),
      body: Padding(
        padding: const EdgeInsets.all(24),
        child: SignalBuilder(
          builder: (context) => Column(
            crossAxisAlignment: CrossAxisAlignment.stretch,
            children: [
              TextField(
                controller: _cityController,
                onChanged: _onCityChanged,
                decoration: const InputDecoration(
                  hintText: 'Berlin, Tokyo, Paris…',
                  labelText: 'City',
                ),
              ),
              const SizedBox(height: 16),
              ElevatedButton(
                onPressed: _onFetchPressed,
                child: const Text('Get forecast'),
              ),
              const SizedBox(height: 24),
              if (loading.value) const CircularProgressIndicator(),
              if (_hasError(error.value))
                Text(
                  _errorText(error.value),
                  style: const TextStyle(color: Color(0xFFB3261E)),
                ),
              if (_hasForecast(forecast.value))
                Column(
                  mainAxisSize: MainAxisSize.min,
                  children: [
                    Text(
                      _temperatureText(forecast.value),
                      style: const TextStyle(
                        fontSize: 40,
                        fontWeight: FontWeight.bold,
                      ),
                    ),
                    Text(_windText(forecast.value)),
                  ],
                ),
            ],
          ),
        ),
      ),
    );
  }
}

// n_url
String _urlFor(String city) {
  return forecastUrl(city);
}

// n_hasError
bool _hasError(String? message) => message != null;
// n_errorText
String _errorText(String? message) => message ?? '';
// n_hasForecast
bool _hasForecast(Forecast? forecast) => forecast != null;
// n_tempText
String _temperatureText(Forecast? forecast) =>
    forecast == null ? '—' : '${forecast.currentWeather.temperature} °C';
// n_windText
String _windText(Forecast? forecast) =>
    forecast == null ? '' : 'Wind ${forecast.currentWeather.windspeed} km/h';
